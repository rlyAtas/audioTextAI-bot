name: 🚀 Deploy to Timeweb

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Проверка переменных
        run: |
          echo "Прямой доступ к vars.SERVER_IP: ${{ vars.SERVER_IP }}"
          echo "Прямой доступ к vars.SERVER_USER: ${{ vars.SERVER_USER }}"
          echo "Через env переменную SERVER_IP: $SERVER_IP"
          echo "Через env переменную SERVER_USER: $SERVER_USER"
          echo "Длина SERVER_IP: ${#SERVER_IP}"
          echo "Длина SERVER_USER: ${#SERVER_USER}"

          if [ -n "$SERVER_IP" ]; then
            echo "✅ SERVER_IP установлен"
          else
            echo "❌ SERVER_IP пуст или не установлен"
          fi

          if [ -n "$SERVER_USER" ]; then
            echo "✅ SERVER_USER установлен"
          else
            echo "❌ SERVER_USER пуст или не установлен"
          fi
        env:
          SERVER_IP: ${{ vars.SERVER_IP }}
          SERVER_USER: ${{ vars.SERVER_USER }}

      - name: 🔐 Подключение и деплой
        uses: appleboy/ssh-action@v1.1.0
        with:
          host: ${{ vars.SERVER_IP }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /root/audioTextAI
            echo "📥 Pull latest code"
            git pull origin main

            echo "📦 Install deps"
            npm install

            echo "🛠 Build TypeScript"
            npm run build

            echo "🗃 Apply Prisma migrations"
            npx prisma migrate deploy

            echo "⚙️ Generate Prisma client"
            npx prisma generate

            echo "🚀 Restart with PM2"
            pm2 restart audiotextai_bot
